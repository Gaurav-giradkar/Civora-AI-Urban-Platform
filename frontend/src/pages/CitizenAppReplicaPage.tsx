/**
 * Civora Fleet Simulator (Phase 2)
 *
 * Replaces the legacy citizen app replica with the real Civora software bus simulator.
 */
import React from 'react';
import { FleetSimulatorPage } from './FleetSimulatorPage';

export const CitizenAppReplicaPage: React.FC = () => {
  return <FleetSimulatorPage />;
};

export default CitizenAppReplicaPage;
